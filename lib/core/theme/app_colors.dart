import 'package:flutter/material.dart';

abstract final class AppColors {
  static const background = Color(0xFF0A0E14);
  static const surface = Color(0xFF111722);
  static const surfaceHigh = Color(0xFF172031);
  static const border = Color(0xFF1F2A3A);

  /// Flutter sky blue.
  static const accent = Color(0xFF54C5F8);
  static const accentSecondary = Color(0xFF8A7CFF);
  static const whatsApp = Color(0xFF25D366);

  static const textPrimary = Color(0xFFE6EDF3);
  static const textSecondary = Color(0xFF9AA7B8);
  static const textMuted = Color(0xFF5C6B7F);

  // Syntax colors for the hero code card.
  static const codeKeyword = Color(0xFFC792EA);
  static const codeClass = Color(0xFFFFCB6B);
  static const codeString = Color(0xFFC3E88D);
  static const codeField = Color(0xFF82AAFF);
}
